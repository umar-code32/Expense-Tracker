DATABASE_URL="file:./dev.db"

# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
AUTH_SECRET=""

# Gmail account used to send signup verification codes (OTP), via the Gmail
# API (OAuth2). See "Email verification" in README.md for full setup steps.
GMAIL_USER=""
GMAIL_CLIENT_ID=""
GMAIL_CLIENT_SECRET=""
GMAIL_REFRESH_TOKEN=""
